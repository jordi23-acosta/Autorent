<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:AutoRent.Views"
       x:Class="AutoRent.AppShell">
  <Shell.Resources>
    <!-- Colores y estilos globales -->
  </Shell.Resources>

  <TabBar>
    <ShellContent Title="Autos" ContentTemplate="{DataTemplate views:CarsPage}" />
    <ShellContent Title="Admin" ContentTemplate="{DataTemplate views:AdminPanelPage}" />
  </TabBar>

  <!-- Rutas adicionales -->
  <Shell.RouteHost>
    <!-- placeholder -->
  </Shell.RouteHost>

  <!-- Register routes in code-behind for clarity -->

</Shell>
