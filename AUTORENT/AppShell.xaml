<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="AUTORENT.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:AUTORENT"
    xmlns:pages="clr-namespace:AUTORENT.Pages"
    x:Name="AppShellRoot"
    Title="AUTORENT"
    Shell.FlyoutBehavior="Disabled">

    <!-- Tabs para CONDUCTOR -->
    <TabBar x:Name="DriverTabs">
        <ShellContent
            Title="Inicio"
            Icon="tab_home.svg"
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />

        <ShellContent
            Title="Mis Rentas"
            Icon="tab_rentals.svg"
            ContentTemplate="{DataTemplate local:RentalsPage}"
            Route="RentalsPage" />

        <ShellContent
            Title="Perfil"
            Icon="tab_profile.svg"
            ContentTemplate="{DataTemplate local:ProfilePage}"
            Route="ProfilePage" />
    </TabBar>

    <!-- Tabs para PROPIETARIO -->
    <TabBar x:Name="OwnerTabs">
        <ShellContent
            Title="Mis Autos"
            Icon="tab_cars.svg"
            ContentTemplate="{DataTemplate pages:MyVehiclesPage}"
            Route="MyVehiclesPage" />

        <ShellContent
            Title="Solicitudes"
            Icon="tab_rentals.svg"
            ContentTemplate="{DataTemplate local:RentalsPage}"
            Route="OwnerRentalsTab" />

        <ShellContent
            Title="Perfil"
            Icon="tab_profile.svg"
            ContentTemplate="{DataTemplate local:ProfilePage}"
            Route="OwnerProfilePage" />
    </TabBar>

</Shell>
